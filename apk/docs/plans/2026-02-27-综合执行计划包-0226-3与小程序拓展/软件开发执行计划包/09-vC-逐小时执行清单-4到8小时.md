# vC 逐小时执行清单（4-8小时）

> 目标：让 Web 端 TTS 可自检、可诊断，PWA 更新可控。

## 0. 开始前（15分钟）

- [ ] 进入目录：`mario-minecraft-game_V1/apk`
- [ ] 新建分支：`git checkout -b feature/vC-web-tts-pwa`
- [ ] 基线检查：`git status`
- [ ] 启动本地：`npm run dev`

通过标准：
1. 分支创建成功
2. 本地服务正常启动

## 1. 第1小时：TTS 诊断 API

文件：`src/modules/03-audio.js`

- [ ] 新增 `diagnoseTts()`
- [ ] 返回字段：
  - [ ] `hasSpeech`
  - [ ] `audioUnlocked`
  - [ ] `speechEnabled`
  - [ ] `providerHint`
- [ ] 挂到 `window.diagnoseTts`

验证：
- [ ] 控制台执行 `window.diagnoseTts()` 有结构化结果

通过标准：
1. 接口存在
2. 返回字段齐全

## 2. 第2小时：设置页“语音自检”入口

文件：
1. `Game.html`
2. `src/modules/16-events.js`
3. `src/styles.css`

- [ ] 新增按钮：`#btn-tts-self-check`
- [ ] 新增结果区：`#tts-self-check-result`
- [ ] 点击后调用 `diagnoseTts()` 并显示可读文本
- [ ] 异常时显示失败提示，不阻塞设置页

验证：
- [ ] 按钮可点击
- [ ] 结果可见

通过标准：
1. 用户不打开控制台也能做语音自检

## 3. 第3小时：SW 注册补齐

文件：`src/modules/17-bootstrap.js`

- [ ] 判断 `serviceWorker in navigator`
- [ ] 注册 `./service-worker.js`
- [ ] 失败仅 `console.warn`，不阻塞游戏

验证：
- [ ] DevTools Application 中能看到 SW

通过标准：
1. SW 注册流程存在且容错

## 4. 第4小时：版本缓存策略

文件：
1. `service-worker.js`
2. `version.json`

- [ ] 缓存名绑定版本号
- [ ] activate 阶段清理旧缓存
- [ ] 新版本发布后走新缓存

验证：
- [ ] 升版本号后刷新资源不再读旧缓存

通过标准：
1. 更新后资源一致性可控

## 5. 第5小时：自动化回归

- [ ] 运行：
```bash
npx playwright test -c tests/e2e/playwright.config.mjs tests/e2e/specs/opt-0226-task7-pwa.spec.mjs
```
- [ ] 若新增 vC 用例，一并执行

通过标准：
1. PWA 相关用例通过或有明确失败记录

## 6. 第6小时：手工回归

- [ ] 设置页语音自检可用
- [ ] 发音正常时返回正常状态
- [ ] 发音异常时返回可读诊断
- [ ] 主流程不回归（开局、答题、存档）

## 7. 第7-8小时（可选）：收尾与提交

- [ ] 按功能分提交：
  - [ ] `feat(vC): add tts diagnose api and self-check ui`
  - [ ] `fix(vC): register service worker in bootstrap`
  - [ ] `fix(vC): versionize cache and cleanup old caches`
- [ ] 更新 `03-vC-P1-WebTTS与PWA-step-by-step.md`
- [ ] 记录已知问题（如果有）

## vC 完成定义（DoD）

- [ ] TTS 自检可用
- [ ] 诊断结果可读
- [ ] SW 注册正常
- [ ] 缓存更新可控
- [ ] 自动化 + 手工回归完成

## 关系补充（vA -> vC）

1. vC 复用 vA 的“测试发音”入口，不新增第二个入口按钮。
2. vC 只增加诊断信息展示和复制能力。

## SW 兼容补充

1. 若检测到 Native/WebView 运行环境，跳过 SW 注册。
2. SW 问题不应影响 APK 主流程。
