# vB 逐小时执行清单（6-10小时）

> 用法：按时间块执行。每块完成后打勾，不要跨块并行。

## 0. 开始前（15分钟）

- [ ] 进入目录：`mario-minecraft-game_V1/apk`
- [ ] 新建分支：`git checkout -b feature/vB-learning-feedback`
- [ ] 基线检查：`git status`
- [ ] 可选基线测试（注意：`opt-0226-task6-achievement-progress.spec.mjs` 需在 vB 开发中创建，首次执行时跳过该文件）：
```bash
npx playwright test -c tests/e2e/playwright.config.mjs tests/e2e/specs/opt-0226-task5-learning-stats.spec.mjs
```

通过标准：
1. 分支创建成功
2. 基线状态可记录

## 1. 第1-2小时：学习统计增强（趋势 + 周对比）

### 1.1 数据结构与渲染点确认（20分钟）

文件：`src/modules/08-account.js`

- [ ] 找到 `renderLearningStats(account)`
- [ ] 确认现有 `wordStats`、`learnedWords`、`stats` 字段可用范围

### 1.2 增加“最近7天学习趋势”模块（45分钟）

文件：
1. `src/modules/08-account.js`
2. `src/styles.css`

- [ ] 在统计区域新增趋势容器（文本或简易条形）
- [ ] 空数据展示“暂无学习记录”
- [ ] 样式不破坏移动端布局

### 1.3 增加“本周 vs 上周”对比（35分钟）

文件：`src/modules/08-account.js`

- [ ] 聚合本周、上周学习/答题数据
- [ ] 输出对比文案（建议含↑/↓）
- [ ] 无上周数据时显示兜底文案

### 1.4 快速验证（20分钟）

- [ ] 打开档案页，确认趋势与周对比可见
- [ ] 空数据账号不报错

通过标准：
1. 档案页新增两个信息块
2. 无 NaN/undefined

## 2. 第3-4小时：弱词行动入口 + 成就扩展

### 2.1 弱词专项入口（30分钟）

文件：`src/modules/08-account.js`

- [ ] 在弱词区新增按钮：`弱词专项练习`
- [ ] 本期直接实现可用 MVP：Top 5 弱词专项练习入口（真实可用）

### 2.2 新成就定义（45分钟）

文件：`src/modules/01-config.js`

- [ ] 新增成就：`streak_3`、`streak_7`、`perfect_run`、`night_owl`
- [ ] 每个成就加 `metric` 与 `target`

### 2.3 进度映射接通（35分钟）

文件：`src/modules/08-account.js`

- [ ] 确认 `getAchievementProgress()` 使用 `metric`
- [ ] 若无对应字段，补最小字段初始化

### 2.4 快速验证（20分钟）

- [ ] 档案页能看到新增成就
- [ ] 进度条显示正常

通过标准：
1. 新成就可见
2. 不出现“有成就无进度”的空壳状态

## 3. 第5-6小时：挑战答题音效（答对/答错/连击）

### 3.1 音效函数实现（40分钟）

文件：`src/modules/03-audio.js`

- [ ] 新增 `playChallengeCorrectSfx(combo)`
- [ ] 新增 `playChallengeWrongSfx()`
- [ ] 挂到 `window`（与现有风格一致）

### 3.2 挑战结果接入（35分钟）

文件：`src/modules/12-challenges.js`

- [ ] 在 `completeLearningChallenge(correct)` 中调用对应音效
- [ ] 连击时传入 streak 参数

### 3.3 设置开关接入（35分钟）

文件：
1. `Game.html`
2. `src/modules/16-events.js`
3. （若需要）`src/modules/09-vocab.js` 或 settings 归一化位置

- [ ] 增加设置项：`挑战音效`
- [ ] 开启/关闭后即时生效

### 3.4 快速验证（10分钟）

- [ ] 答对/答错音效不同
- [ ] 关闭开关后无音效

通过标准：
1. 音效可区分
2. 开关可控

## 4. 第7小时：自动化回归

- [ ] 运行：
```bash
npx playwright test -c tests/e2e/playwright.config.mjs tests/e2e/specs/opt-0226-task5-learning-stats.spec.mjs tests/e2e/specs/opt-0226-task6-achievement-progress.spec.mjs
```
（注意：`opt-0226-task6-achievement-progress.spec.mjs` 需在本版本 Task 2 完成后创建，若尚未创建则先跳过）

通过标准：
1. 关键用例通过
2. 失败项有记录与修复动作

## 5. 第8小时：手工回归

- [ ] 趋势与周对比可见
- [ ] 新成就和进度可见
- [ ] 答题音效可开关
- [ ] 主流程无回归（开局、答题、存档、读取）

## 6. 第9-10小时（可选）：收尾与提交

- [ ] 按功能分3次提交（建议）
  - [ ] `feat(vB): add learning trend and weekly comparison`
  - [ ] `feat(vB): expand achievements with progress mapping`
  - [ ] `feat(vB): add challenge sfx and settings toggle`
- [ ] 更新文档：`02-vB-P1-学习体验增强-step-by-step.md`
- [ ] 记录已知风险与后续待办

## vB 完成定义（DoD）

- [ ] 学习统计增强可见
- [ ] 成就深化可追踪
- [ ] 音效反馈可用且可关闭
- [ ] 自动化 + 手工回归完成
- [ ] 可进入 vC
